# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
      displayName: 'Install Node.js'

    - script: |
        cd tic-tac-toe/
        echo "Current directory is now $(pwd)"
        npm install
        echo "Install package successfully"
        npm run build
        echo "Build successfully"
        
        # Debug: List directories to find where build output went
        echo "Directory structure after build:"
        ls -la
      displayName: 'npm install and build'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: 'tic-tac-toe/dist'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
        replaceExistingArchive: true
      displayName: 'Archive build files'

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'drop'
        publishLocation: 'Container'
      displayName: 'Publish artifacts'



# - stage: Deploy
#   dependsOn: Build
#   jobs:
#   - deployment: DeployToVM
#     environment:
#       name: 'New environment'
#       resourceType: VirtualMachine
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           - download: current
#             artifact: drop
            
#           - script: |
#               # Extract the zip file
#               unzip $(Pipeline.Workspace)/drop/$(Build.BuildId).zip -d /var/www/html/tic-tac-toe
              
#               # If you need to restart a service (e.g., nginx)
#               # sudo systemctl restart nginx
              
#               echo "Deployment completed successfully"
#             displayName: 'Deploy to VM'